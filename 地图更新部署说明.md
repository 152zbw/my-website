# 地图更新部署说明

本文档用于将本地最新代码同步到服务器 `/var/www/ec-int`，避免依赖服务器直接 `git pull` 访问 GitHub。

## 适用场景

- 服务器执行 `git pull` 经常超时
- 本地代码已经修改并提交到仓库
- 需要让 `www.ec-int.cn` 立刻使用最新代码

## 当前线上项目

`www.ec-int.cn` 当前由服务器上的以下项目提供服务：

- 项目目录：`/var/www/ec-int`
- PM2 服务名：`ec-int`
- 监听端口：`3000`

## 推荐更新方式

### 方式一 本地同步文件到服务器

如果服务器无法稳定拉取 GitHub，建议直接将本地最新文件同步到服务器。

#### 需要同步的文件

本次地图相关改动主要涉及：

- `html/admin/map-settings.html`
- `html/js/map.js`
- `routes/websiteInfo.js`
- `models/WebsiteInfo.js`
- `init-db.js`

如果你后续还有别的改动，也可以一并同步对应文件。

#### 操作步骤

在本地电脑执行：

```bash
scp "/Users/zade/Desktop/JW 2/html/admin/map-settings.html" root@47.104.69.36:/var/www/ec-int/html/admin/map-settings.html
scp "/Users/zade/Desktop/JW 2/html/js/map.js" root@47.104.69.36:/var/www/ec-int/html/js/map.js
scp "/Users/zade/Desktop/JW 2/routes/websiteInfo.js" root@47.104.69.36:/var/www/ec-int/routes/websiteInfo.js
scp "/Users/zade/Desktop/JW 2/models/WebsiteInfo.js" root@47.104.69.36:/var/www/ec-int/models/WebsiteInfo.js
scp "/Users/zade/Desktop/JW 2/init-db.js" root@47.104.69.36:/var/www/ec-int/init-db.js
```

然后登录服务器重启服务：

```bash
ssh root@47.104.69.36
cd /var/www/ec-int
pm2 restart ec-int
pm2 save
```

### 方式二 服务器重新拉取

如果服务器网络恢复，可以直接执行：

```bash
cd /var/www/ec-int
git pull
pm2 restart ec-int
pm2 save
```

但如果再次出现 GitHub 443 超时，请改用方式一。

## 注意事项

- 确认本地仓库已经是最新提交
- 上传前最好先检查文件修改是否完整
- 上传后务必重启 `ec-int`
- 如果页面仍不生效，先强制刷新浏览器缓存

## 当前地图相关逻辑

后台地图页已经改成：

- 使用百度地图 JS API
- 预览和前台使用同一份地图配置
- 保存时写入后端网站信息
- 预览失败时有明确错误提示

如果地图预览仍异常，优先检查：

1. 百度地图 AK 是否正确
2. Referer 白名单是否已放行 `ec-int.cn` 和 `www.ec-int.cn`
3. 服务器上的 `/var/www/ec-int` 是否已经收到最新文件
4. `pm2 restart ec-int` 是否已执行
